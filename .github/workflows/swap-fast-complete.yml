name: "Swap: Fast Complete (10 jobs)"
on:
  workflow_dispatch:

jobs:
  fast-1: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-1" }] }
  fast-2: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-2" }] }
  fast-3: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-3" }] }
  fast-4: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-4" }] }
  fast-5: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-5" }] }
  fast-6: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-6" }] }
  fast-7: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-7" }] }
  fast-8: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-8" }] }
  fast-9: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-9" }] }
  fast-10: { runs-on: tenki-staging-standard-autoscale, steps: [{ run: "sleep 5 && echo fast-10" }] }
